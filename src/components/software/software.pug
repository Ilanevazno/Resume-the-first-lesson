include ../block-header/block-header
include ../progress/progress
mixin software(data = {})
  .software
    .software__header
      +block-header({src: 'software-icon.jpg', title: 'Software', modif: 'right_side'})
    .software__main
      each item in data
        +progress({ name: item.name, percent: item.percent })