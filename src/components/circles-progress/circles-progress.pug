mixin circles-progress({ filled = 0, count = 0 })
  ul.circles-progress
    - for (let x = 0; x < filled; x += 1)
      li.circles-progress__item.circles-progress__item_filled
    - while (count - filled > 0)
      li.circles-progress__item
      - count -= 1;