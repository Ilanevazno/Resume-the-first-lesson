mixin circles-progress(data = {})
  -
    let {
      filled = 0,
      count = 0
    } = data;
  ul(class="circles")
    - for (let x = 0; x < filled; x += 1)
      li.circles__item_filled
    - while (count - filled > 0)
      li.circles__item
      - count -= 1;