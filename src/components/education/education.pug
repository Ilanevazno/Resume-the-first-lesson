include ../block-header/block-header
include ../practice/practice
mixin education(data = {})
  .education
    .education__header
      +block-header({src: 'education-icon.jpg', title: 'Education', theme: 'right-side'})
    .education__main
      +practice(data, 'blue')