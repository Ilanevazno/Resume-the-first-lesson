include ../block-header/block-header
include ../practice/practice
mixin education(data = {})
  .education
    .education__header
      +block-header({src: 'education-icon.jpg', title: 'Education', modif: 'right_side'})
    .education__main
      +practice(data, data.theme = 'blue')