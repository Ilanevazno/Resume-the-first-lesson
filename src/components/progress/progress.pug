mixin progress(data = {})
  -
    const {
      name = '',
      percent = 0,
    } = data;
  .progress
    p(class=`progress__title-${name}`)= name
    .progress__block
      div(class="progress__block-inner" style=`width:${percent}%`)